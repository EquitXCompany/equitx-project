{
  "name": "equitx-stability-pool-indexer",
  "version": "0.0.1",
  "description": "Indexes CDPs and stakes in the stability pool",
  "main": "dist/index.js",
  "scripts": {
    "build": "subql codegen && subql build",
    "codegen": "subql codegen",
    "start:docker": "docker-compose pull && docker-compose up --remove-orphans",
    "dev": "subql codegen && subql build && docker-compose pull && docker-compose up --remove-orphans",
    "prepack": "rm -rf dist && npm run build",
    "test": "subql build && subql-node-stellar test",
    "build:develop": "NODE_ENV=develop subql codegen && NODE_ENV=develop subql build"
  },
  "homepage": "https://github.com/subquery/stellar-subql-starter",
  "repository": "github:subquery/stellar-subql-starter",
  "files": [
    "dist",
    "schema.graphql",
    "project.yaml"
  ],
  "author": "Aha Labs",
  "license": "MIT",
  "dependencies": {
    "@stellar/stellar-sdk": "^13.0.0",
    "@subql/common": "latest",
    "@subql/types-soroban": "^2.2.3-2",
    "@subql/types-stellar": "latest"
  },
  "devDependencies": {
    "@subql/cli": "latest",
    "@subql/testing": "latest",
    "@subql/types": "latest",
    "dotenv": "latest",
    "typescript": "^5.2.2"
  }
}
